{
  "name": "test-app",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-packager . musicPlayer --win --out ./dist version=2.0.7"
  },
  "author": "zwq",
  "license": "ISC",
  "devDependencies": {
    "electron": "^2.0.7"
  },
  "dependencies": {
    "electron-packager": "^12.1.1"
  }
}
